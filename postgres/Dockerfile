# Use the official PostgreSQL image from Docker Hub
FROM postgres:latest

# Set the necessary environment variables
ENV POSTGRES_USER=sonarqube
ENV POSTGRES_PASSWORD=sonarqube
ENV POSTGRES_DB=sonarqube
